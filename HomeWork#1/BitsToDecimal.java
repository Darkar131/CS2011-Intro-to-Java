import java.util.Scanner;

public class BitsToDecimal {

    public static void main(String args[]) {
       Scanner input = new Scanner(System.in); //Set Scanner

       System.out.print("Enter a binary number: "); //User Prompt 
       int bits =input.nextInt();

       bits = (binary) bits; 

       //System.out.println(bits + " in binary is "+ Integer.parseInt(bits, 2) + " in decimal");
       System.out.println(bits " in binary is " + conversionToDecimal + " in decimal");
    
    input.close();   
    }
}

